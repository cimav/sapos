= form_for(@student, :url => { :action => "create_student_mobility" }) do |f|
  = hidden_field_tag 'student_id', @student.id
  = hidden_field_tag 'student[student_mobilities_attributes][0][student_id]', @student.id
  = fields_for "student[student_mobilities_attributes][0]" do |fields|
    #mobility-new

      %div{:id => "field_student_mobilities_0_institution", :class => 'field'}
        = fields.label :institution, 'Institución'
        = fields.text_field :institution

      %div{:id => "field_studentf_mobilities_0_start_date", :class => 'field'}
        = fields.label :start_date, 'Inicio'
        = fields.date_select :start_date

      %div{:id => "field_student_mobilities_0_end_date", :class => 'field'}
        = fields.label :end_date, 'Fin'
        = fields.date_select :end_date

      %div{:id => "field_student_mobilities_0_activities", :class => 'field'}
        = fields.label :activities, 'Actividades'
        = fields.text_area :activities

      #dialog-submit-button-area
        = fields.submit "Crear nueva mobilidad"

